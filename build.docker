#!/usr/bin/env bash

if [ $# -ne 1 ]; then
  echo "Usage: $0 image:tag[ecr.vip.ebayc3.com/wenliu2/ssh-on-web:1.0.x]"
  exit -1
fi
PWD=`cd $(dirname $0);pwd`
cd $PWD

IT=$1

docker build -t ${IT} .
docker login -u 'victor2333' -p 'private007'
docker push ${IT}